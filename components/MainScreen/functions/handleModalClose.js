// Handler for MainScreen Settings modal close

/**
 * 
 * @param {Function} setModalState - Setter for modal state
 */
export default function handleModalClose (setModalState) {
    setModalState(false);
}