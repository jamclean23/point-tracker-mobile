// Handle cleanup when ForgotPassword modal closes

// ====== FUNCTIONS ======

export default function handleForgotPasswordModalClose (setShowForgotPasswordModal) {
    setShowForgotPasswordModal(false);
}